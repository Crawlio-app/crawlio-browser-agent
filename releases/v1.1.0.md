# v1.1.0

**Date:** 2026-02-24

## Changes

### Changed

- Replaced broad `host_permissions` with narrow `http://127.0.0.1/*` — eliminates CWS delayed review
- Removed `content_scripts` manifest entry
- Migrated all MCP tool handlers from `chrome.scripting.executeScript` to CDP `Runtime.evaluate`
- Enrichment accumulator now uses CDP for framework detection and DOM capture

### Added

- `cdpExecuteFunction<T>()` helper — executes functions via CDP `Runtime.evaluate`, bypassing the `chrome.scripting` permission gate
