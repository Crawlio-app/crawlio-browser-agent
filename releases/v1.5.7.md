# v1.5.7

**Date:** 2026-03-09

## Changes

### Added

- **100 tools** in full mode: 93 browser + 3 extraction + 3 session recording + 1 compiler
- Code mode now exposes **133 searchable commands** via the `search` tool
- 4 new smart methods: `detectTables()`, `extractTable()`, `waitForNetworkIdle()`, `extractData()`
- 4 new full-mode tools: `detect_tables`, `extract_table`, `extract_data`, `wait_for_network_idle`
- 7 new structured data types: `TableCandidate`, `TableColumn`, `ExtractedRow`, `TableExtraction`, `NetworkIdleResult`, `DataExtraction`
- 11th comparison dimension: `data-structure` for structured data diffing
- IDS-inspired table detection scores elements by `area × children²` to find data-rich regions
- Recursive path-keyed extraction algorithm for column detection
- 17 new full-mode tool tests (537 total)

### Technical Details

Zero new permissions — all extraction via existing `debugger` + `Runtime`/`Network` CDP domains.
